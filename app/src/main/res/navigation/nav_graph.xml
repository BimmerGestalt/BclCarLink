<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ConnectionStatusFragment">

    <fragment
        android:id="@+id/ConnectionStatusFragment"
        android:name="io.bimmergestalt.bclclient.ConnectionStatusFragment"
        android:label="@string/connection_status_fragment_label"
        tools:layout="@layout/fragment_connection_status">

        <action
            android:id="@+id/action_ConnectionStatusFragment_to_SettingsFragment"
            app:destination="@id/SettingsFragment" />
    </fragment>
    <fragment
        android:id="@+id/SettingsFragment"
        android:name="io.bimmergestalt.bclclient.SettingsFragment"
        android:label="@string/settings_fragment_label"
        tools:layout="@layout/fragment_settings">
    </fragment>
</navigation>